server:
  port: 8761

spring:
  application:
    name: discovery-management-service

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
logging:
  file:
    name: E:/logs/discovery-management-service.log